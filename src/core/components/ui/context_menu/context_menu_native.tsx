import type * as React from "react";

/** Web fallback for the platform-native ContextMenu implementation. */
export function ContextMenuNative(_props: Record<string, unknown>): React.ReactElement | null {
  return null;
}
