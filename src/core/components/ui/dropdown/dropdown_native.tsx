import * as React from "react";

/** Web fallback for the platform-specific native Dropdown implementation. */
export function DropdownNative(_props: Record<string, unknown>): React.ReactElement | null {
  return null;
}
