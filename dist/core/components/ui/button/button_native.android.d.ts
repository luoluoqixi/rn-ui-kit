import type * as React from "react";
import type { ButtonNativeProps } from "./button_native";
/** Expo UI Jetpack Compose renderer used by the Android native Button mode. */
export declare function ButtonNative({ androidColors, buttonSize, children, disabled: isDisabled, nativeComposeProps, onPress, style, title, variant, }: ButtonNativeProps): React.JSX.Element;
