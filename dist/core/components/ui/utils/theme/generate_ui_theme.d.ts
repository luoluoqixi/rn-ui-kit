import type { UiThemeConfig } from "./ui_theme";
/** Generates the complete light/dark semantic theme from an opaque hex seed color. */
export declare function generateUiThemeFromPrimaryColor(primaryColor: string): UiThemeConfig;
