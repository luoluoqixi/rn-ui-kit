# Tamagui NativePortal 补丁说明:
# - 为 NativePortal 提供稳定的逐项名称，用于基于 teleport 的 scoped portal host。
# - 需要与 @tamagui+portal@2.7.7.patch 保持同步。
diff --git a/dist/esm/components.native.js b/dist/esm/components.native.js
index a080ce9376580c4a2d299087f422794b021a628f..e81266886becee4788791ba5be4b509edaf1397d 100644
--- a/dist/esm/components.native.js
+++ b/dist/esm/components.native.js
@@ -1,10 +1,12 @@
 import { jsx as _jsx, Fragment as _Fragment } from "react/jsx-runtime";
+import { useId } from "react";
 import { getPortal } from "./portalState.native.js";
 function NativePortal(param) {
   var {
     hostName = "root",
     children
   } = param;
+  var portalName = useId();
   var state = getPortal().state;
   if (state.type !== "teleport") return null;
   var {
@@ -12,6 +14,7 @@ function NativePortal(param) {
   } = globalThis.__tamagui_teleport;
   return /* @__PURE__ */_jsx(Portal, {
     hostName,
+    name: portalName,
     children
   });
 }
diff --git a/src/components.tsx b/src/components.tsx
index c632a5de7a275fac70a6e983303be5da7c9d6164..825b48e5f51f8776a5420928b431ab97057efb51 100644
--- a/src/components.tsx
+++ b/src/components.tsx
@@ -1,4 +1,4 @@
-import type { ReactNode } from 'react'
+import { useId, type ReactNode } from 'react'
 import { getPortal } from './portalState'
 import type {
   NativePortalProps,
@@ -16,11 +16,16 @@ export function NativePortal({
   hostName = 'root',
   children,
 }: NativePortalProps): ReactNode {
+  const portalName = useId()
   const state = getPortal().state
   if (state.type !== 'teleport') return null
 
   const { Portal } = (globalThis as any).__tamagui_teleport
-  return <Portal hostName={hostName}>{children}</Portal>
+  return (
+    <Portal hostName={hostName} name={portalName}>
+      {children}
+    </Portal>
+  )
 }
 
 /**
