# Tamagui portal host 补丁说明:
# - 增加 scoped portal root host 支持，使 Dialog/AlertDialog/Menu 可以渲染在当前激活的
#   sheet overlay host 内部。
# - 调整前需要重新验证 sheet 内 overlay 的 pointer events 行为。
diff --git a/dist/esm/GorhomPortal.native.js b/dist/esm/GorhomPortal.native.js
index 42ff03c6dbea7fc77dfd50b21a3c3fda10643fa0..6ed27e999a25f03d3b6394009e445da1e5a19b47 100644
--- a/dist/esm/GorhomPortal.native.js
+++ b/dist/esm/GorhomPortal.native.js
@@ -69,6 +69,20 @@ var reducer = function (state, action) {
 var PortalStateContext = /* @__PURE__ */createContext(null);
 var PortalDispatchContext = /* @__PURE__ */createContext(null);
 var PortalProviderActiveContext = /* @__PURE__ */createContext(false);
+var PortalRootHostContext = /* @__PURE__ */createContext("root");
+var usePortalRootHostName = function () {
+  return useContext(PortalRootHostContext) || "root";
+};
+var PortalRootHostProvider = function (param) {
+  var {
+    hostName = "root",
+    children
+  } = param;
+  return /* @__PURE__ */jsx(PortalRootHostContext.Provider, {
+    value: hostName,
+    children
+  });
+};
 var usePortalState = function (hostName) {
   var state = useContext(PortalStateContext);
   if (state === null) throw new Error("'PortalStateContext' cannot be null, please add 'PortalProvider' to the root component.");
@@ -201,5 +215,5 @@ function PortalHostNonNative(props) {
     }, item.name);
   }));
 }
-export { ACTIONS, INITIAL_STATE, PortalHost, PortalProvider, usePortal };
+export { ACTIONS, INITIAL_STATE, PortalHost, PortalProvider, PortalRootHostProvider, usePortal, usePortalRootHostName };
 //# sourceMappingURL=GorhomPortal.native.js.map
diff --git a/dist/esm/GorhomPortalItem.native.js b/dist/esm/GorhomPortalItem.native.js
index e716ecd29f4af1dc3a2e3cafc3c2cb9e18eb270c..b9a82d7f0c1134f171c0678f921d15febaad1fe5 100644
--- a/dist/esm/GorhomPortalItem.native.js
+++ b/dist/esm/GorhomPortalItem.native.js
@@ -4,7 +4,7 @@ import { useEvent } from "@tamagui/core";
 import { getPortal, NativePortal } from "@tamagui/native";
 import { Theme, useThemeName } from "@tamagui/web";
 import { useEffect, useId } from "react";
-import { usePortal } from "./GorhomPortal.native.js";
+import { usePortal, usePortalRootHostName } from "./GorhomPortal.native.js";
 var GorhomPortalItem = function (props) {
   var {
     name: _providedName,
@@ -15,19 +15,21 @@ var GorhomPortalItem = function (props) {
     children,
     passThrough
   } = props;
+  var scopedRootHostName = usePortalRootHostName();
+  var resolvedHostName = hostName === "root" ? scopedRootHostName : hostName;
   var portalState = getPortal().state;
   if (portalState.type === "teleport") {
     if (passThrough) {
       return children;
     }
     return /* @__PURE__ */_jsx(NativePortal, {
-      hostName,
+      hostName: resolvedHostName,
       children
     });
   }
   return /* @__PURE__ */_jsx(GorhomPortalItemFallback, {
     name: _providedName,
-    hostName,
+    hostName: resolvedHostName,
     handleOnMount: _providedHandleOnMount,
     handleOnUnmount: _providedHandleOnUnmount,
     handleOnUpdate: _providedHandleOnUpdate,
diff --git a/dist/esm/Portal.native.js b/dist/esm/Portal.native.js
index 619095a055d19d35d2f02a136407d35b69efda83..63527805ad5db986fb690f2dbbac190dc8b02fd2 100644
--- a/dist/esm/Portal.native.js
+++ b/dist/esm/Portal.native.js
@@ -2,6 +2,7 @@ import { jsx as _jsx } from "react/jsx-runtime";
 import { View } from "@tamagui/core";
 import { getPortal, NativePortal } from "@tamagui/native";
 import { useStackedZIndex } from "@tamagui/z-index-stack";
+import { usePortalRootHostName } from "./GorhomPortal.native.js";
 import { GorhomPortalItem } from "./GorhomPortalItem.native.js";
 import { getStackedZIndexProps } from "./helpers.native.js";
 var Portal = function (propsIn) {
@@ -19,16 +20,17 @@ var Portal = function (propsIn) {
     passThrough,
     children
   });
+  var rootHostName = usePortalRootHostName();
   var portalState = getPortal().state;
   if (portalState.type === "teleport") {
     return /* @__PURE__ */_jsx(NativePortal, {
-      hostName: "root",
+      hostName: rootHostName,
       children: contents
     });
   }
   return /* @__PURE__ */_jsx(GorhomPortalItem, {
     passThrough,
-    hostName: "root",
+    hostName: rootHostName,
     children: contents
   });
 };
diff --git a/src/GorhomPortal.tsx b/src/GorhomPortal.tsx
index 32915f54d46e838451af75dc5e9b02691074faba..b55e15cc8adfdb63a87dc3afc077a99ed67d1103 100644
--- a/src/GorhomPortal.tsx
+++ b/src/GorhomPortal.tsx
@@ -145,6 +145,23 @@ const reducer = (state: Record<string, Array<PortalType>>, action: ActionTypes)
 const PortalStateContext = createContext<Record<string, Array<PortalType>> | null>(null)
 const PortalDispatchContext = createContext<React.Dispatch<ActionTypes> | null>(null)
 const PortalProviderActiveContext = createContext(false)
+const PortalRootHostContext = createContext('root')
+
+export const usePortalRootHostName = () => {
+  return useContext(PortalRootHostContext) || 'root'
+}
+
+export interface PortalRootHostProviderProps {
+  hostName?: string
+  children: ReactNode | ReactNode[]
+}
+
+export const PortalRootHostProvider = ({
+  hostName = 'root',
+  children,
+}: PortalRootHostProviderProps) => {
+  return <PortalRootHostContext.Provider value={hostName}>{children}</PortalRootHostContext.Provider>
+}
 
 const usePortalState = (hostName: string) => {
   const state = useContext(PortalStateContext)
diff --git a/src/GorhomPortalItem.native.tsx b/src/GorhomPortalItem.native.tsx
index 7ef6f096e3c9f9e2c24b3080a4c821dd437d1624..411ec22c90360dec38cecf40259f8f90723d920c 100644
--- a/src/GorhomPortalItem.native.tsx
+++ b/src/GorhomPortalItem.native.tsx
@@ -6,7 +6,7 @@ import { useEvent } from '@tamagui/core'
 import { getPortal, NativePortal } from '@tamagui/native'
 import { Theme, useThemeName } from '@tamagui/web'
 import { useEffect, useId } from 'react'
-import { usePortal } from './GorhomPortal'
+import { usePortal, usePortalRootHostName } from './GorhomPortal'
 import type { PortalItemProps } from './types'
 
 export const GorhomPortalItem = (props: PortalItemProps) => {
@@ -20,6 +20,9 @@ export const GorhomPortalItem = (props: PortalItemProps) => {
     passThrough,
   } = props
 
+  const scopedRootHostName = usePortalRootHostName()
+  const resolvedHostName = hostName === 'root' ? scopedRootHostName : hostName
+
   const portalState = getPortal().state
 
   // use teleport if available - it preserves context so we can skip the Gorhom system
@@ -27,14 +30,14 @@ export const GorhomPortalItem = (props: PortalItemProps) => {
     if (passThrough) {
       return children
     }
-    return <NativePortal hostName={hostName}>{children}</NativePortal>
+    return <NativePortal hostName={resolvedHostName}>{children}</NativePortal>
   }
 
   // fall back to Gorhom portal system
   return (
     <GorhomPortalItemFallback
       name={_providedName}
-      hostName={hostName}
+      hostName={resolvedHostName}
       handleOnMount={_providedHandleOnMount}
       handleOnUnmount={_providedHandleOnUnmount}
       handleOnUpdate={_providedHandleOnUpdate}
diff --git a/src/Portal.native.tsx b/src/Portal.native.tsx
index ecea03c2ee2d2821bd006bc64b08548bac913bdd..ad91a4676a486bbbebb3ed0561a55afa6ce7da76 100644
--- a/src/Portal.native.tsx
+++ b/src/Portal.native.tsx
@@ -1,6 +1,7 @@
 import { View } from '@tamagui/core'
 import { getPortal, NativePortal } from '@tamagui/native'
 import { useStackedZIndex } from '@tamagui/z-index-stack'
+import { usePortalRootHostName } from './GorhomPortal'
 import { GorhomPortalItem } from './GorhomPortalItem'
 import { getStackedZIndexProps } from './helpers'
 import type { PortalProps } from './PortalProps'
@@ -22,16 +23,18 @@ export const Portal = (propsIn: PortalProps) => {
     </View>
   )
 
+  const rootHostName = usePortalRootHostName()
+
   const portalState = getPortal().state
 
   // use teleport if available (best option - preserves context)
   if (portalState.type === 'teleport') {
-    return <NativePortal hostName="root">{contents}</NativePortal>
+    return <NativePortal hostName={rootHostName}>{contents}</NativePortal>
   }
 
   // fall back to Gorhom portal system (JS-based, needs context re-propagation)
   return (
-    <GorhomPortalItem passThrough={passThrough} hostName="root">
+    <GorhomPortalItem passThrough={passThrough} hostName={rootHostName}>
       {contents}
     </GorhomPortalItem>
   )
diff --git a/types/GorhomPortal.d.ts b/types/GorhomPortal.d.ts
index c68dfa65b61fdb444f3038474f6abe9448b89d1e..3ec4305412ffa51c3d19e1b7ab5a8a8cf69a0bfd 100644
--- a/types/GorhomPortal.d.ts
+++ b/types/GorhomPortal.d.ts
@@ -53,6 +53,12 @@ export interface PortalProviderProps {
     children: ReactNode | ReactNode[];
 }
 export declare const PortalProvider: React.MemoExoticComponent<({ rootHostName, shouldAddRootHost, children, }: PortalProviderProps) => import("react/jsx-runtime").JSX.Element>;
+export interface PortalRootHostProviderProps {
+    hostName?: string;
+    children: ReactNode | ReactNode[];
+}
+export declare const PortalRootHostProvider: ({ hostName, children, }: PortalRootHostProviderProps) => import("react/jsx-runtime").JSX.Element;
+export declare const usePortalRootHostName: () => string;
 export interface PortalHostProps {
     /**
      * Host's key or name to be used as an identifier.
